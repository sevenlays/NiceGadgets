/* Типізацію в подальшому буде винесено в глобальні типи поки залишаю тут щоб не було конффліктів*/

export type Product = {
  id: number;
  category: string;
  itemId: string;
  name: string;
  fullPrice: number;
  price: number;
  screen: string;
  capacity: string;
  color: string;
  ram: string;
  year: number;
  image: string;
};
